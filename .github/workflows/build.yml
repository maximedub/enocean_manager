name: Build and publish EnOcean Manager

on:
  push:
    branches:
      - master

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    name: Build and Push Docker Image
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
        with:
          platforms: all

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push (amd64)
        uses: docker/build-push-action@v5
        with:
          context: ./enocean_manager
          push: true
          platforms: linux/amd64
          tags: ghcr.io/${{ github.actor }}/enocean_manager:amd64

      - name: Build and push (armv7)
        uses: docker/build-push-action@v5
        with:
          context: ./enocean_manager
          push: true
          platforms: linux/arm/v7
          tags: ghcr.io/${{ github.actor }}/enocean_manager:armv7

      - name: Build and push (arm64)
        uses: docker/build-push-action@v5
        with:
          context: ./enocean_manager
          push: true
          platforms: linux/arm64
          tags: ghcr.io/${{ github.actor }}/enocean_manager:arm64
